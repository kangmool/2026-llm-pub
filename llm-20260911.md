## StreamBase 10.6 커스텀 Java Operator 내 JDBC 접근 및 구현 요약## 1. 핵심 결론

* StreamBase 엔진은 커넥션 풀을 캡슐화하여 제어하므로, Java Operator에서 커넥션 객체(Connection)를 직접 획득하는 공식 API는 제공되지 않음.
* 따라서 요구사항에 따라 TableAccessor 사용, JNDI Lookup, 독립 커넥션 풀(HikariCP 등) 구축 중 하나를 선택해야 함.

## 2. JNDI Lookup이 가능한데 TableAccessor를 쓰는 이유
JNDI 방식을 통해 표준 JDBC(Batch 처리 등)를 자유롭게 쓸 수 있음에도 TableAccessor를 사용하는 이유는 다음과 같은 엔진 아키텍처적 이점 때문임.

* 트랜잭션 생명주기 일치: 엔진의 트랜잭션 매니저와 동기화되어, 데이터 흐름(Flow) 에러 시 전체 롤백 처리가 가능함. (JNDI는 독단적 제어로 인해 롤백 불가)
* Tuple 타입 호환성: StreamBase Tuple 객체를 별도의 매핑 코드 없이 테이블 레코드로 즉시 바인딩하여 생산성이 높음.
* 엔진 내부 모니터링: 쿼리 수행 및 커넥션 대기 시간을 엔진(sb-admin 등)이 추적할 수 있어 블랙박스 문제를 방지함.
* 컴파일 시점 검증: typecheck() 단계에서 테이블 존재 여부와 스키마 일치성을 미리 검증하므로 안정이 높음.

## 3. Batch Insert 구현 방향성

* TableAccessor 방식 (권장): Java 코드로 수동 루프를 돌리지 않아도, HOCON 설정(batchSize, batchTimeoutMillis)에 따라 엔진이 알아서 배치 처리를 수행함.
* JNDI Lookup 방식: DB 드라이버 전용 옵션 미세 튜닝이나 복잡한 SQL 제어가 필수적인 고성능 시나리오에서만 표준 executeBatch() 코드를 직접 구현하여 사용함.

------------------------------
현재 구현하려는 Batch Insert의 목표 처리량(초당 데이터 건수)이나 사용하는 DBMS 종류(Oracle, MySQL 등)를 공유해 주시면, 성능을 최적화할 수 있는 구체적인 설정 가이드나 코드 스니펫을 지원해 드리겠습니다.

